//微信支付
function editWxPay(event) {
    return showPostFormModal(event, 'editWxPayForm', null, true);
}

//支付宝支付
function editAliPay(event) {
    return showPostFormModal(event, 'editAliPayForm', null, true);
}