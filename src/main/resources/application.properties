# Thymeleaf
spring.thymeleaf.cache: false

# DataSource settings
spring.datasource.url=jdbc:mysql://localhost/joyspace_test?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true
spring.datasource.username=joyspace
spring.datasource.password=guf:5Jj*bv
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# server setting
server.port = 6060
server.session.persistent=true
server.session.store-dir=./session_store
server.session.timeout=31536000

# logging
logging.level.ROOT: INFO
logging.file: ./log/joyspace.log

server.use-forward-headers=true

# 上传限制
spring.http.multipart.max-file-size=10MB
spring.http.multipart.max-request-size=10MB

com.unicolour.joyspace.assetsDir=F:/Temp/JoySpace/public/assets

com.unicolour.wxAppId=
com.unicolour.wxMchId=
com.unicolour.wxAppSecret=
com.unicolour.wxPayKey=